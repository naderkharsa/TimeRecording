<core:FragmentDefinition xmlns="sap.m"
    xmlns:core="sap.ui.core">
    <Dialog id="StopDialog" title="{i18n>dlgStopTitle}">
        <VBox class="sapUiSmallMargin">
            <Text text="{i18n>txtWorkDesc}" />
            <Input id="shortTextInput" value="{/entry/shortText}" liveChange=".onDialogInputChange" placeholder="{i18n>phShortText}" class="sapUiTinyMarginBottom" />

            <Label text="{i18n>lblAccounting}" />
            <Select id="accountingSelect" selectedKey="{/entry/accountingId}" forceSelection="false" change=".onDialogInputChange">
                <core:Item key="01" text="{i18n>accounting01}" />
                <core:Item key="02" text="{i18n>accounting02}" />
            </Select>
        </VBox>

        <beginButton>
            <Button id="okBtn" text="{i18n>btnOK}" press=".onStopConfirm" enabled="false" />
        </beginButton>

        <endButton>
            <Button text="{i18n>btnCancel}" press=".onStopCancel" />
        </endButton>
    </Dialog>
</core:FragmentDefinition>
